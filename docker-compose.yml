services:
  mongo:
    image: mongo:7
    container_name: petcare-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - petcare_mongo_data:/data/db

volumes:
  petcare_mongo_data:
