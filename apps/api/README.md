# PetCare API

Backend REST para o MVP PetCare Responsável.

## Scripts

- `npm run dev` — desenvolvimento com hot reload
- `npm run build` — compila TypeScript
- `npm run start` — produção
- `npm run seed` — popula 132 fichas em 13 grupos de animais
- `npm run lint` — ESLint

## Endpoints

Ver `../../docs/API.md`.

## Variáveis

Copie `.env.example` para `.env`.
